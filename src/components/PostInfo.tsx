"use client";

import Image from "./Image";

const PostInfo = () => {
  return (
    <div className="relative size-4 cursor-pointer">
      <Image src="icons/infoMore.svg" alt="" w={16} h={16} />
    </div>
  );
};

export default PostInfo;
